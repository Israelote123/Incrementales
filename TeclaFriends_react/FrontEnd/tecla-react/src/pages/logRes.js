import { FormLogin } from "../components/FormLogin/FormLogin";

function login() {
    return (
        <FormLogin />
      );
}

function register() {
    return (
        <FormRegister />
      );
}

export {login, register};
